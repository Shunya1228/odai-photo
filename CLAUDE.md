# odai-photo

PWA「お題写真ガチャ」アプリ。1人モード MVP。

## このプロジェクトについて

AI(Gemini)がランダムなお題を出してくれて、撮った写真を採点してもらうアプリ。
さんぽ神テイストの「日常に刺激を与える」体験を目指す。

全体設計は **`design.md`** に記載。Claude Code は実装前に必ず該当セクションを読むこと。

---

## 開発者の学習スタイル(最重要)

開発者は Next.js を久しぶりに触る状態。**実装スピードよりも理解度を優先**してください。

### 必ず守ること

1. **フェーズごとに一度止まる**
   - 1フェーズ完了したら、次のフェーズに進む前に必ず止まる
   - 「次のフェーズに進んでよいか?」を明示的に確認する
   - 開発者から「次へ」「進めて」などの明確な GO サインを待つ

2. **実装前に「これから何をするか」を解説**
   - 各フェーズの最初に、これから実装する内容の概要を説明
   - 使う技術・ライブラリ・概念を簡潔に紹介
   - なぜそうするのかの理由も添える

3. **実装後に「何をしたか」を解説**
   - 各フェーズ完了時に、書いたコードの要点を解説
   - 特に重要な部分(セキュリティ・パフォーマンス・型安全性など)を強調
   - 開発者が「ふーん、なるほど」と納得できるレベルで説明

4. **質問タイムを明示的に設ける**
   - 各フェーズの解説の後、「ここまでで質問あれば聞いてください」と明示
   - 質問が出たら丁寧に回答してから次へ進む
   - 「無ければ次のフェーズへ進みますが、よろしいですか?」で締める

5. **専門用語は初出時に1行で説明**
   - 例: 「Server Action(Next.js の機能で、API ルートを書かずにサーバー処理を呼べる仕組み)を使います」
   - すでに過去のフェーズで説明したものは省略 OK

### フェーズ進行の流れ(テンプレート)

```
1. 「Phase X を開始します。これから○○を実装します」
2. 概要説明(何を、なぜ、どう作るか)
3. 実装(必要なら段階的に)
4. 「実装完了しました。要点を解説します」
5. コードの要点解説
6. 「ここまでで質問あれば聞いてください。無ければ次のフェーズへ進みますか?」
7. 開発者の GO サインを待つ
```

---

## プロジェクトルール

### 技術スタックのバージョン(厳守)
- Node.js: **22 LTS 以降(24 LTS 推奨)**
- Next.js: **16.2.6 以降**(または 15.5.18 以降)
- React: **19.2.6 以降**
- TypeScript: latest
- パッケージマネージャ: **pnpm**(10.x 以降)

**インストール前に必ず `npm view next version` で最新を確認**してから決める。
詳細は `design.md` §2 を参照。

### コーディング規約

- **型は `any` 禁止**。`unknown` + 型ガードを使う
- **`as` 型アサーション最小限**。型を狭めたいときは型ガードを書く
- **Server Action は必ず zod でバリデーション**(入力検証)
- **Gemini API キーは絶対にクライアント側に流出させない**
  - `NEXT_PUBLIC_` 接頭辞は環境変数につけない
  - Server Action 内のみで使用
- **コメントは必要なところに**(なぜこうしたかが伝わる程度に)
- **コミットメッセージは Conventional Commits**(feat:, fix:, chore:, docs: など)

### セキュリティ(最重要)

実装中、常に `design.md` §10 を意識すること。特に:

- Next.js / React のバージョン要件を絶対に下げない
- Middleware を使わない(MVP では認証なし、CVE-2025-29927 の教訓)
- すべての Server Action で zod バリデーション
- レート制限を必ず実装(Gemini 無料枠を守るため)
- セキュリティヘッダーを `next.config.ts` で設定

リリース前に **`design.md` §10.7 のチェックリスト** を全項目確認。

### フェーズの進め方

`design.md` §12 のフェーズ順で進める。

- Phase 0: UI デザイン(Claude Code 内で各フェーズと並行して作成)
- Phase 1: 基盤セットアップ
- Phase 2: Gemini クライアント + お題生成
- Phase 3: 写真撮影 + プレビュー
- Phase 4: 採点機能
- Phase 5: レート制限 + セキュリティ仕上げ
- Phase 6: PWA 化
- Phase 7: UX 仕上げ

**1フェーズ完了ごとに必ずコミット + 動作確認 + 質問タイム**。

---

## 実装中のお願い

### 不明点があれば実装前に質問
- 設計に曖昧な部分があれば、推測せず開発者に確認
- 「以下の解釈で進めますがよろしいですか?」のように確認する
- 勝手に仕様を追加・変更しない

### エラーが出たら一緒に考える
- エラーメッセージを隠さずに見せる
- 原因の仮説を立ててから対応する
- 「とりあえず動いた」で済ませない、根本原因を理解する

### ライブラリ追加は事前確認
- `design.md` §2 に記載のもの以外を追加する場合、必ず事前に説明・許可を取る
- ライブラリの選定理由・代替案・セキュリティ面のリスクを説明する

### 開発者がつまずきやすいポイントは丁寧に
- Server Components / Client Components の使い分け(`'use client'` のタイミング)
- Server Action の挙動と裏側の仕組み
- TypeScript の型定義(特に zod スキーマからの型推論)
- async / await と Promise の扱い
- ファイルアップロード(FormData の使い方)

これらが出てきたら、軽くでも仕組みの解説を入れる。

---

## ファイル構成

詳細は `design.md` §11 を参照。

主要ディレクトリ:
- `src/app/` — ページとレイアウト、Server Actions
- `src/components/` — UI コンポーネント
- `src/lib/` — ユーティリティ、外部 API クライアント
- `src/types/` — 型定義
- `public/` — 静的アセット(manifest.json、アイコン)

---

## 動作確認の方法

各フェーズ完了時、以下を実行して動作確認:

```bash
pnpm install        # 依存関係インストール
pnpm dev            # 開発サーバー起動(localhost:3000)
pnpm build          # 本番ビルド確認
pnpm lint           # Lint チェック
pnpm audit          # セキュリティ監査
```

動作確認手順は各フェーズで開発者に明示すること。

---

## コミュニケーションスタイル

- 日本語で対話する
- 専門用語は初出時に補足、2回目以降は普通に使う
- 開発者を急かさない。ペース配分は開発者に合わせる
- 開発者の質問には誠実に答える(分からないことは「分からない」と言う)
- 推測で答えない。確証がない場合は調べるか、明示的に「推測ですが」と前置きする

---

## 開発者の背景情報(参考)

- 普段は Python / バックエンド寄りの業務エンジニア
- Next.js は久しぶり、最新の App Router や Server Components は初学者に近い
- セキュリティに対する意識は高い
- 「動けばいい」よりも「理解して進めたい」タイプ
- 設計をしっかり詰めてから実装に入るスタイル

---

## このプロジェクトを通じて学びたいこと

開発者が習得したいトピック(参考):
- Next.js App Router の基本
- React Server Components / Server Actions
- TypeScript + zod のバリデーション設計
- Web の最新セキュリティ事情(脆弱性対応の考え方)
- PWA 実装の基本
- LLM API(Gemini)の使い方とプロンプト設計

これらが出てきたフェーズでは特に丁寧に解説する。
